# 插件重构计划（MVP 先行）

> 注：该计划已完成，保留作为历史记录。

## 目标
先完成最小功能：稳定选中所有弹幕节点，优先覆盖普通直播与全屏抓取内容，活动页适配先搁置，再逐步推进发送与交互。

## 分步执行
1. 第一步（已完成）：弹幕节点覆盖
- 使用容器候选链自动探测弹幕容器
- 使用 MutationObserver 监听新增节点并绑定事件
- 使用定时补扫兜底，降低漏绑
- 通过浏览器探针采样普通页与全屏场景 DOM
- 活动页适配已完成（iframe 场景）

2. 第二步：弹幕内容解析增强
- 统一解析文本 + emoji + 大表情
- 补全 data-name / alt 等字段提取

3. 第三步：发送链路稳定性
- 输入框定位兼容
- 冷却与失败重试策略

4. 第四步：调试能力
- 增加 observer 命中计数、容器命中信息
- 输出关键状态方便回归

## 第一步验收标准
- 普通页与全屏场景可命中并显示 +1 按钮
- 高频弹幕下无明显漏绑
- 全屏切换后可自动恢复命中
